#include <bits/stdc++.h>
#define pb push_back
#define mp make_pair
#define pp pair<ll, int>
#define ppp pair<int, pp>
#define fi first
#define se second
#define esp 1e-15
#define inf 1000000001
#define mod 1000000007
#define N 200010
#define base 311097
typedef long long ll;
typedef long double ld;
const long long oo = (ll)1e18;
using namespace std;

int p[123];
int final_ant(std::vector<int> arr) {
    int n = arr.size();
    for (int i = 1; i <= n; i++)
        p[i] = arr[i];
    while (true) {
        for (int i = 1; i <= n; i++)
        if (p[i] != 0) {
            if (p[i] > 0) {

            }
        }
    }
}

int main() {
    //freopen("in.in", "r", stdin);
    //freopen("ou.ou", "w", stdout);
    ios::sync_with_stdio(false);

    /**/ return 0;
}
